---
description: General project rules and conventions
alwaysApply: true
---

# Project Rules

## Tech Stack
- Next.js 16 with App Router
- TypeScript strict mode
- Tailwind CSS
- Supabase
- React Query
- Vercel AI SDK

## Conventions
- Use functional React components
- Use TypeScript interfaces
- Use async/await instead of `.then()` chains
- Use kebab-case for file names
- Use PascalCase for component names
- Avoid `any` types
- Keep business logic outside UI components

## Folder Structure
- `app/` - App Router routes, layouts and API routes

### Auth
- `app/(auth)/login/` - login route
- `app/(auth)/register/` - register route

### Patient dashboard
- `app/dashboard/` - patient home
- `app/dashboard/checkin/` - daily check-in
- `app/dashboard/questions/` - patient questions
- `app/dashboard/context/` - patient read-only zorgcontext
- `app/dashboard/advice/` - DailyBuddy advice
- `app/dashboard/link/` - patient account linking

### Care dashboard
- `app/care/` - caregiver overview (aggregate stats + primary navigation)
- `app/care/patients/` - complete clinical patient list (admission-status filters)
- `app/care/patients/[patientId]/` - patient detail (admit/edit/context)
- Future `Mijn patiënten` (department- or caregiver-scoped) is not implemented; do not rename the complete list to that label

### Planning module
- `app/planning/` - coordinator daily participation dashboard
- `app/planning/volunteers/` - volunteer roster overview

### Volunteer module
- `app/volunteer/` - volunteer daily view
- `app/volunteer/availability/` - block availability
- `app/volunteer/profile/` - volunteer profile

### Admin module
- `app/admin/` - admin home
- `app/admin/users/` - user management
- `app/admin/roles/` - role management
- `app/admin/departments/` - department reference data

### Shared
- `components/ui/` - small reusable UI primitives
- `components/layout/` - layout components
- `components/dashboard/` - domain-specific view components
- `components/forms/` - reusable form components
- `hooks/` - custom React hooks and React Query hooks
- `lib/` - application logic, integrations and utilities
- `lib/supabase/` - Supabase clients and helpers
- `lib/services/` - data access and business logic
- `lib/ai/` - AI agents and AI configuration
- `lib/tools/` - AI tool calling functions
- `lib/constants/` - shared constants
- `lib/utils/` - utility functions
- `types/` - shared TypeScript type definitions
- `providers/` - React context providers
- `public/` - static assets

## Scope
- Build OpnameBuddy as a patient recovery participation dashboard.
- Do not add features outside the requested scope.
- Do not implement medical diagnosis or treatment recommendation logic.
- AI may only support recovery participation within caregiver-defined boundaries.